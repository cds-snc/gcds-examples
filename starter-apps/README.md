# Starter Apps

This folder contains example **starter projects** for various frameworks and setups, intended to serve as a foundation when bootstrapping new projects using our design system.

_Future / Planned_ We hope to eventually integrate these into a CLI tool (`create-starter-app`) to simplify project setup.

## 📦 What's Inside

Each folder in this directory is a complete starter project for a specific framework or setup:

- `vue/` - Starter app built with Vue 3 + Vite
- `react/` - Starter app built with React + TypeScript + Vite
- `html/` - Starter built with vanilla HTML + Vite
- `angular/` - *(coming soon)*

Each starter includes:

- Basic layout and navigation
- Integration with the GC Design System
- Sample components and usage
- Linting, formatting, testing, and recommended tooling setup
